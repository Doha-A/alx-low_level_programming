#include "main.h"

/**
 * _abs - a function returning the absloute value of the number
 * @r: integre
 * Return: returns the absloute value
*/

int _abs(int r)
{
	int r;

	if (r < 0)
		r = r * -1;
	return (r);
}

