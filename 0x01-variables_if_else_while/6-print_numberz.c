#include <stdio.h>

/**
 * main - A program that prints the numbers from 0 to 9
 * Return: returning 0 success
*/

int main(void)
{
	int n;

	n = n + '0';
	for (n = 0; n <= 9; n++)
		putchar(n);
	putchar('\n');
	return (0);
}
