#include <stdio.h>

/**
 * main - A program that pt=rints the alphapet in lowercase
 * Return: returning 0 success
*/

int main(void)
{
	char ch = 'a';

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	return (0);
}
